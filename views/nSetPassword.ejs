<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Password - AlumNet</title>
    <link rel="stylesheet" href="newResponsiveHomepage.css">
    <link rel="Webpage Icon" href="/PSIT_logo_green.svg" style="fill: green;">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Work+Sans&display=swap" rel="stylesheet">
    <!-- Link to your CSS file for this page -->
    <style>
        /* Import your common styles from ResponsiveHomepage.css */

        /* Specific styles for the new password page */
        body {
            background-image: linear-gradient(rgba(4, 9, 30, 0.7), rgba(4, 9, 30, 0.7)), url(/psit\ kanpur.jpg);
            background-position: center;
            background-size: cover;
            background-attachment: fixed; /* Fixed background image */
        }

        .password-box {
            text-align: center;
            background-color: rgba(65, 63, 63, 0.614); /* Semi-transparent background */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
            margin: 0 auto;
            width: 80%;
            max-width: 400px; /* Adjust the maximum width as needed */
        }

        .password-box h1 {
            color: #2ecc70;
            font-size: 40px;
            margin-bottom: 20px;
        }

        .password-form input {
            width: 100%;
            margin: 10px 0;
            padding: 10px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            background-color: rgba(254, 254, 254, 0.107); /* Semi-transparent input background */
            color: #fff;
        }

        .password-form button {
            width: 100%;
            padding: 12px;
            font-size: 18px;
            background: #2ecc70;
            border: none;
            border-radius: 5px;
            color: #fff;
            cursor: pointer;
            transition: background 0.5s;
        }

        .password-form button:hover {
            background: #2ecc70c7; /* Slightly transparent on hover */
        }

        /* Media query for responsiveness */
        @media (max-width: 700px) {
            .password-box {
                width: 85%; /* Adjust width for smaller screens */
                padding: 10px; /* Reduce padding for smaller screens */
                padding-top: 40px;
            }

            .password-form input {
                width: 90%;
                margin: 10px 1px; /* Reduce margin for smaller screens */
                padding: 8px;
                font-size: 20px; /* Adjust font size for smaller screens */
            }

            .password-form button {
                padding: 10px;
                font-size: 20px; /* Adjust font size for smaller screens */
                width: 95%;
                margin: 10px;
            }

            .password-box h1 {
                color: #2ecc70;
                font-size: 30px;
                margin-bottom: 20px;
            }
        }
        @media (min-width: 700px) {
            .password-box {
                width: 85%; /* Adjust width for smaller screens */
                padding: 10px; /* Reduce padding for smaller screens */
                padding-top: 60px;
            }

            .password-form input {
                width: 90%;
                margin: 10px 1px; /* Reduce margin for smaller screens */
                padding: 8px;
                font-size: 20px; /* Adjust font size for smaller screens */
            }

            .password-form button {
                padding: 10px;
                font-size: 20px; /* Adjust font size for smaller screens */
                width: 95%;
                margin: 10px;
            }

            .password-box h1 {
                color: #2ecc70;
                font-size: 30px;
                margin-bottom: 20px;
            }
        }

        /* Styling for the "Already have an account?" text and "Login" link */
        .password-box p {
            margin-top: 15px; /* Add some space between the create button and the text */
            margin-bottom: 10px;
            color: #fff; /* Text color */
            font-size: 14px; /* Font size */
        }

        .password-box a {
            color: #2ecc70; /* Link color */
            text-decoration: none; /* Remove underlines from the link */
            font-weight: bold; /* Make the link text bold */
            margin-left: 5px; /* Add some space between the text and the link */
        }

        .password-box a:hover {
            text-decoration: underline; /* Underline the link on hover */
        }

        /* Input field focus and hover styles */
        .password-form input:focus {
            border: 2px solid #2ecc70; /* Change border color on focus */
            background-color: rgba(254, 254, 254, 0.2); /* Change background color on focus */
        }

        .password-form input:hover {
            background-color: rgba(254, 254, 254, 0.2); /* Change background color on hover */
        }

        /* Button focus and hover styles */
        .password-form button:focus {
            background: #2ecc70c7; /* Change background color on focus */
        }

        .password-form button:hover {
            background: #2ecc70c7; /* Slightly transparent on hover */
            transition: background 0.5s;
        }

        .password-box p {
            margin-top: 0px; /* Add some space between the create button and the text */
            margin-bottom: 0px; /* Reduce space between the two paragraphs */
            color: #fff; /* Text color */
            font-size: 14px; /* Font size */
        }
        .error-message {
            color: #ff0000; /* Change the text color to red */
        }
        body{
            font-family: 'Work Sans', sans-serif;
        }
    </style>
</head>
<body>
    <section class="header">
        <!-- Header content from your "Get Started" page -->
        <nav>
            <img src="/PSIT_logo_green.svg" alt="psit-logo" id="psit-logo">
            <h1><u>Alum</u><u class="ul"><span>Net</u></span></h1>
        </nav>
    </section>

    <section class="text-box password-box">
        <h1>Create New Password</h1>
        <div id="error-message" class="error-message"><%= message %></div>
        <form class="password-form" action="/generatePassword" method="POST">
            <input type="password" name="newPassword" placeholder="New Password" required>
            <input type="password" name="confirmNewPassword" placeholder="Confirm New Password" required>
            <button class="hero-btn" type="submit">Reset Password</button>
        </form>
        <p class="ptag">Already have an account? <a href="login.html">Login</a></p>
    </section>

    <!-- Footer and other sections if needed -->

</body>
</html>
